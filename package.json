{
  "name": "panel-telefederal-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud web control panel for vMix with a local bridge.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
